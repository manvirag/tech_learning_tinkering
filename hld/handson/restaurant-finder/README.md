rectange is visualise as bottom left point with its width and height. 

we are putting the elements in quad tree with logic
- if top rectage has capacity fill it. 
- else break and fill the child. 
- for finding, with point and radius. 
- make the rectange with 2x size by moving x-radius and y-radius.
- start from top, check intersection if yes, for each point keep which are actually intersection, and now do same for child.
package main

import (
	"external_stl/stl"
)

func main() {
	// // Run all examples from stl package
	// stl.Vector()
	// fmt.Println("\n" + strings.Repeat("=", 50))
	// stl.Stack()
	// fmt.Println("\n" + strings.Repeat("=", 50))
	// stl.Queue()
	// fmt.Println("\n" + strings.Repeat("=", 50))
	// stl.UnorderedSet()
	// fmt.Println("\n" + strings.Repeat("=", 50))
	// stl.UnorderedMap()
	stl.Set()
}

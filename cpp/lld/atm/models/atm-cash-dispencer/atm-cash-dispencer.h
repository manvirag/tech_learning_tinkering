#include<string>
#include<vector>

class AtmCashDispencer{
    public:
      void CollectCash(std::vector<std::string> notes); // for simplicity assume note is an string for e.g. "500" -> a note of 500 rupess
};